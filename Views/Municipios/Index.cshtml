@model IEnumerable<CRUDATM.Models.Municipios>

@{
    ViewBag.Title = "Index";
}

<h2>Municipios</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MunNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MunNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MunID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MunID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MunID })
        </td>
    </tr>
}

</table>
