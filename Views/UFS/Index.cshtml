@model IEnumerable<CRUDATM.Models.UFS>

@{
    ViewBag.Title = "Index";
}

<h2>UF</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UfSigla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UfNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UfSigla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UfNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UfID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UfID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UfID })
        </td>
    </tr>
}

</table>
